<Window x:Class="DKLock.App.Dialogs.PasswordSetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create master password" Width="460" Height="360" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" ShowInTaskbar="False" Style="{StaticResource DialogWindow}">
    <Grid Margin="28">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <TextBlock Text="Create master password" FontSize="24" FontWeight="SemiBold"/>
        <TextBlock Grid.Row="1" Margin="0,8,0,20" Style="{StaticResource SecondaryText}" Text="This credential protects application unlock requests. DK LOCK stores an Argon2id verifier, not the plaintext password."/>
        <TextBlock Grid.Row="2" Text="Master password" Margin="0,0,0,6"/>
        <PasswordBox x:Name="Password" Grid.Row="3" Height="38" Style="{StaticResource SecurePasswordBox}"/>
        <StackPanel Grid.Row="4" Margin="0,14,0,0">
            <TextBlock Text="Confirm password" Margin="0,0,0,6"/>
            <PasswordBox x:Name="Confirm" Height="38" Style="{StaticResource SecurePasswordBox}"/>
            <TextBlock x:Name="ErrorText" Foreground="{StaticResource Brush.Error}" Margin="0,10,0,0" TextWrapping="Wrap"/>
        </StackPanel>
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Save password" Style="{StaticResource PrimaryButton}" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
